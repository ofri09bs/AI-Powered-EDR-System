In order to use data collection via winAPI, you must first run main.py, 
and then run (preferably as administrator) dataCollectorAgent.exe and let it run in the background 
The source code was originaly compiled in Visual Studio.

Note: it is not yet finished, so a terminal window with debugging messages will open.